// Type
// Easy way to refer to the different properties + functions that a value has

const today = new Date();
today.getMonth();

const person = {
  age: 20,
};

class Color {
  derp() {
    console.log("hii");
  }
}

const red = new Color();
red.derp();
